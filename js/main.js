const gallery=document.getElementById('gallery'),filters=document.getElementById('filters'),lb=document.getElementById('lightbox'),lbImg=document.getElementById('lightboxImg');let current=0,shown=[];
const cats=['All',...new Set(photos.map(p=>p.category))];
cats.forEach((c,i)=>{const b=document.createElement('button');b.className='filter'+(i===0?' active':'');b.textContent=c;b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(c)};filters.appendChild(b)});
function render(cat='All'){gallery.innerHTML='';shown=photos.filter(p=>cat==='All'||p.category===cat);shown.forEach((p,i)=>{const el=document.createElement('figure');el.className='portfolio-card';el.innerHTML=`<img src="${p.file}" alt="${p.title}"><div class="caption"><strong>${p.title}</strong><br><small>${p.location}</small></div>`;el.onclick=()=>openLb(i);gallery.appendChild(el)})}
function openLb(i){current=i;lbImg.src=shown[current].file;lb.classList.add('open')}function closeLb(){lb.classList.remove('open')}function step(n){current=(current+n+shown.length)%shown.length;lbImg.src=shown[current].file}
document.getElementById('close').onclick=closeLb;document.getElementById('prev').onclick=()=>step(-1);document.getElementById('next').onclick=()=>step(1);lb.onclick=e=>{if(e.target===lb)closeLb()};document.addEventListener('keydown',e=>{if(!lb.classList.contains('open'))return;if(e.key==='Escape')closeLb();if(e.key==='ArrowLeft')step(-1);if(e.key==='ArrowRight')step(1)});render();